{
    "configurations": [
        {
            "name": "Python: gunicorn",
            "type": "debugpy",
            "request": "launch",
            "module": "gunicorn",
            "args": [
                "main:app",
                "--host", "localhost",
                "--port", "8080",
                "--reload"
            ],
            "cwd": "${workspaceFolder}/code/src",
            "justMyCode": true,
            "env": {
                "PYTHONPATH": "${workspaceFolder}/code/src"
            }
        }
    ]
}
